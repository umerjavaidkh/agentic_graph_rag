# Backwards-compatible override for PDF ingest commands.
# PDF ingest is now lightweight in the default image too.
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.full
    environment:
      ENABLE_PDF_INGEST: "true"
