# Document Knowledge Graph — Dependencies
# pip install -r requirements.txt

# ── Document parsing ─────────────────────
docling>=2.5.0
docling-core>=2.10.0
pymupdf>=1.24.0          # page images for optional vision enrichment
Pillow>=10.0.0           # JPEG compression for page assets
minio>=7.2.0             # optional: ASSET_STORAGE_BACKEND=minio

# ── Embeddings + LLM ────────────────────
openai>=1.30.0
fastapi>=0.100.0
uvicorn>=0.24.0
python-multipart>=0.0.6

# ── Semantic analysis ───────────────────
numpy>=1.26.0,<2.0.0       # Pinned <2 for torch/docling compatibility
scikit-learn>=1.4.0        # KMeans clustering for SAME_CATEGORY

# ── Neo4j driver (for live import option) ──
neo4j>=5.20.0

# ── Optional: local embeddings (no API key) ──
# sentence-transformers>=3.0.0