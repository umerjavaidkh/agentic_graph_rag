# Slim image (~500–900 MB) — chat, structured Northwind, API, lightweight PDF ingest.
# No heavy parser runtime.

# ── Lightweight PDF ────────
pymupdf>=1.24.0
pdfplumber>=0.11.0
rtldoc @ git+https://github.com/umerjavaidkh/rtldoc.git@v1.0.1

# ── OCR fallback (low-confidence pages; requires the tesseract-ocr system
# package too — see Dockerfile) ─────────
pytesseract>=0.3.10
Pillow>=10.0.0

# ── Embeddings + LLM ────────────────────
# openai is always required (embeddings + the default chat provider).
# anthropic/google-genai are optional chat providers (MODEL_PROVIDER=anthropic|gemini).
openai>=1.30.0
anthropic>=0.40.0
google-genai>=0.3.0
fastapi>=0.100.0
uvicorn>=0.24.0
python-multipart>=0.0.6
pydantic>=2.5.0

# ── App runtime ─────────────────────────
python-dotenv>=1.0.0
langgraph>=0.2.0

# ── Semantic analysis ───────────────────
numpy>=1.26.0,<2.0.0
scikit-learn>=1.4.0

# ── Neo4j ───────────────────────────────
neo4j>=5.20.0

# ── Scalable ingestion queue (optional — degrades to in-process if unset) ──
redis>=5.0.0
rq>=1.16.0

# ── OIDC / JWT auth (Google default; corporate IdP via OIDC_* env) ────────
PyJWT[crypto]>=2.8.0

# ── Blob storage (optional — degrades to zero-dep local disk if BLOB_STORE_BACKEND unset) ──
minio>=7.2.0
# ── Vector storage (optional — degrades to zero-dep in-process store if
# VECTOR_STORE_BACKEND unset). Missing here (though present in
# requirements.txt) meant VECTOR_STORE_BACKEND=qdrant would crash with
# ModuleNotFoundError in the actual deployed image, since the Dockerfile
# installs from THIS file, not requirements.txt -- same class of miss as
# minio's earlier omission. Root cause of Qdrant sitting empty despite
# running as a container: nothing could ever successfully switch to it.
qdrant-client>=1.9.0
